default:
  dependencies:
    - initial_task
    - new_task

initial_task:
  dependencies: []
  prompt: "Create a Python function that adds two numbers."
  continued_prompt: >
    Based on the outcome of the {{prompt}}, create function headers for other arithmetic operations involving division. Do not implement them.

  # task_generation_prompt: > 
  #   Based on the outcome of the {{prompt}}, generate new targets to create functions for other arithmetic operations involving division.
  
  # ay: "Say hi in a random language."
  ay: "Pick one task from the list of generated targets at random and implement it in Python."

# Add more targets here if needed
imp_task:
  dependencies: []
  prompt: "Pick 2 numbers from 3, 5 and 6."

new_task:
  dependencies: 
    - imp_task
  prompt: >
    From the numbers in "{{imp_task}}", do a mathematical operation on them.

