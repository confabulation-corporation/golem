default:
  dependencies:
    - greet_person

greet_person:
  dependencies: [get_name, get_greeting]
  prompt: "{{get_greeting}} {{get_name}}."

get_name:
  prompt: "Say hi to Farhan."

get_greeting:
  prompt: "Generate a random word starting with W."
