default:
  dependencies:
    - generate_message
  prompt: "The generated message is: '{{generate_message}}'."

generate_message:
  prompt: "Create a unique message that includes the word 'cache'."
  model: gpt-3.5-turbo
